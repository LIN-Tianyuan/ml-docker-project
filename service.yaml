apiVersion: v1
kind: Service
metadata:
  name: iris-classifier-service
spec:
  type: NodePort
  selector:
    app: iris-classifier
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080